const assert = require('assert');
const Uppercase = require('../models/uppercase.js');


describe('Uppercase', function() {

  beforeEach(function() {
    const stringArray = ['hello', 'my', 'name', 'is', 'james']
  })

  it('should return an array of strings to a new array containing uppercase versions of each string')

})
